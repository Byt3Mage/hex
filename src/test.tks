mod math {
    struct Point { x: int, y: int, z: ?Point }

    const LEN = 6;

    fn square(n: int) : int {
        n * n
    }
}

union Result {
    ok: uint,
    err: Status,
}

enum Status {
    Active = 1,
    Inactive,
}

fn sum(arr: [int; 5]): int {
    return 5;
}

fn test(): int {
    let x = if true {
        return 42;
    } else {
        "hello"
    };
    x
}
