mod math {
    struct Point { x: int, y: int, z: ?Point }

    const LEN = 6;

    fn square(n: int) : int {
        n * n
    }
}

union Result {
    ok: uint,
    err: Status,
}

enum Status {
    Active = 1,
    Inactive,
}

fn sum(arr: [int; 5]) : int {
    arr[0] + arr[1] + arr[2] + arr[3] + arr[4]
}

fn make_arr() : [int; 3] {
    let x: [cint; 3] = [1; 3];
    x
}
